# ===================================
#  List Column Definitions
# ===================================

columns:
    form_id:
        label: gromit.forms::lang.models.submission.columns.form_id.label
        relation: form
        select: name
    created_at:
        label: gromit.forms::lang.models.submission.columns.created_at.label
        type: date
        format: d.m.Y H:i
    viewed_at:
        label: gromit.forms::lang.models.submission.columns.viewed_at.label
        type: date
        format: d.m.Y H:i
